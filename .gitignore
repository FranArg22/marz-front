node_modules
.pnpm-store
.DS_Store
dist
dist-ssr
*.local
.env
.env.local
.env.*.local
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
.claude/
coverage
*.log
bun.lock
bun.lockb
# npm lockfile — this repo uses pnpm (pnpm-lock.yaml); never commit npm's
package-lock.json
.rafita/runs/
.rafita/state.json
.rafita/plans/
.rafita/sessions/
test-results/
playwright-report/
playwright/
.playwright-mcp/

# clerk configuration (can include secrets)
/.clerk/

# dev-flow worktree-only patches (orval target rewritten to dynamic backend URL)
orval.config.ts.dev-flow.bak

# generated by i18n standards check
src/__i18n_standards_test_bundled__/

# Claude Code agent memory (debe vivir en ~/.claude, no en el repo)
memory/
