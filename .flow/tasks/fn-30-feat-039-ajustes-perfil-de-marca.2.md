Already written to disk. Key changes: (1) redirect({ to: '/_brand/ajustes/general' }) → redirect({ to: '/ajustes/general' }) in ajustes.index.tsx snippet. (2) redirect({ to: '/_brand/ajustes/suscripcion' }) → redirect({ to: '/ajustes/suscripcion' }) in billing.tsx snippet. (3) Added explicit rule: 'redirect({ to: }) siempre con URL paths, nunca route IDs con layout prefix. Convención del repo.' (4) Clarified guard lives in beforeLoad callback, not useRouteContext.